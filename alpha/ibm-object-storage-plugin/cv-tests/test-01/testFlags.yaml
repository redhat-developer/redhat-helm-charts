# This file contains any flags to specify on helm test.  The key / value pairs
# are translated to command line arguments.
#
# For example, if uncomment the debug:true entry below the helm test will be called
# with '--debug=true' option.
#
#debug: true 
#cleanup: true
